---
---
<button id="back-to-top-btn" aria-label="back to top" class="z-50">
	<span aria-hidden="true">^</span>
</button>

<style>
button {
	@apply w-10 h-10 transition-all border border-black bg-bgColor
	text-textColor fixed bottom-4 sm:bottom-8 right-4 sm:right-8
	hover:bg-black hover:text-bgColor hidden justify-center items-center font-bold text-lg;
}
</style>

<script>
	const backToTopBtn = document.getElementById("back-to-top-btn");
	if (backToTopBtn) {
		backToTopBtn.addEventListener("click", () => {
			document.body.scrollTop = 0; // For Safari
			document.documentElement.scrollTop = 0; // For Chrome, Firefox, IE and Opera
		});
		window.addEventListener("scroll", () => {
			if(window.scrollY < document.documentElement.clientHeight) {
				backToTopBtn.classList.remove("fade-in");
				backToTopBtn.classList.add("fade-out");
				backToTopBtn.style.display = "flex";
			} else {
				backToTopBtn.classList.remove("fade-out");
				backToTopBtn.classList.add("fade-in");
			}
		});
	}

</script>
